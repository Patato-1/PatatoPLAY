# W1 练习代码库

所有练习代码都放这里，按天组织。**每天跟着敲一遍，不要复制粘贴。**

完整代码文件在 Cursor 专栏的 `03_练习代码/` 目录下。

---

## 📅 周一（6/29）练习

### 练习1：自我介绍
```python
name = "你的名字"
age = 25
city = "上海"
print(f"我叫{name}，{age}岁，来自{city}")
print(f"明年我就{age+1}岁了")
```

### 练习2：圆的面积
```python
r = 5
pi = 3.14159
area = pi * r ** 2
print(f"半径{r}的圆面积是{area:.2f}")
```

### 练习3：温度转换
```python
celsius = float(input("请输入摄氏温度: "))
fahrenheit = celsius * 9/5 + 32
print(f"摄氏{celsius}度 = 华氏{fahrenheit:.1f}度")
```

### 练习4：判断闰年
```python
year = int(input("请输入年份: "))
if (year % 4 == 0 and year % 100 != 0) or (year % 400 == 0):
    print(f"{year}年是闰年")
else:
    print(f"{year}年不是闰年")
```

---

## 📅 周二（6/30）练习

### 学生成绩管理
```python
students = [
    {"name": "张三", "score": 85},
    {"name": "李四", "score": 92},
    {"name": "王五", "score": 78}
]

# 打印所有学生
for s in students:
    print(f"{s['name']}: {s['score']}分")

# 计算平均分
avg = sum(s["score"] for s in students) / len(students)
print(f"平均分: {avg:.1f}")

# 最高分
top = max(students, key=lambda s: s["score"])
print(f"最高分: {top['name']}")
```

---

## 📅 周三（7/1）练习

### 猜数字游戏
```python
import random

answer = random.randint(1, 100)
count = 0

while True:
    guess = int(input("猜1-100: "))
    count += 1

    if guess > answer:
        print("大了")
    elif guess < answer:
        print("小了")
    else:
        print(f"对了！用了{count}次")
        break
```

### FizzBuzz（经典面试题）
```python
for i in range(1, 101):
    if i % 15 == 0:
        print("FizzBuzz")
    elif i % 3 == 0:
        print("Fizz")
    elif i % 5 == 0:
        print("Buzz")
    else:
        print(i)
```

### 九九乘法表
```python
for i in range(1, 10):
    for j in range(1, i + 1):
        print(f"{j}*{i}={i*j}", end="\t")
    print()
```

---

## 📅 周四（7/2）练习

### 计算器函数
```python
def add(a, b): return a + b
def sub(a, b): return a - b
def mul(a, b): return a * b
def div(a, b): return a / b if b != 0 else "除数不能为0"

print(add(3, 5))
print(div(10, 0))
```

### 判断素数
```python
def is_prime(n):
    if n < 2:
        return False
    for i in range(2, int(n**0.5) + 1):
        if n % i == 0:
            return False
    return True

primes = [n for n in range(2, 101) if is_prime(n)]
print(f"100以内有{len(primes)}个素数")
```

---

## 📅 周五（7/3）练习

### 5 个独立脚本

#### 1. calculator.py
```python
def calculator():
    while True:
        expr = input("输入表达式 (q退出): ")
        if expr.lower() == 'q':
            break
        try:
            print(f"结果: {eval(expr)}")
        except:
            print("输入有误")

if __name__ == "__main__":
    calculator()
```

#### 2. guess_number.py
```python
import random

def play():
    answer = random.randint(1, 100)
    while True:
        try:
            guess = int(input("猜: "))
            if guess > answer: print("大了")
            elif guess < answer: print("小了")
            else: print("对了！"); break
        except ValueError:
            print("请输入数字")

if __name__ == "__main__":
    play()
```

#### 3. file_rename.py
```python
import os

def batch_rename():
    folder = input("文件夹路径: ")
    if not os.path.isdir(folder):
        return
    prefix = input("新前缀: ")
    files = [f for f in os.listdir(folder) if os.path.isfile(os.path.join(folder, f))]
    for i, f in enumerate(files, 1):
        ext = os.path.splitext(f)[1]
        os.rename(os.path.join(folder, f), os.path.join(folder, f"{prefix}_{i:03d}{ext}"))
    print(f"完成{len(files)}个文件")

if __name__ == "__main__":
    batch_rename()
```

#### 4. list_demo.py
```python
fruits = ["苹果", "香蕉", "橙子"]
fruits.append("葡萄")
fruits.insert(1, "西瓜")

numbers = [3, 1, 4, 1, 5, 9, 2, 6]
print(sorted(numbers))
print(sum(numbers))

squares = [x**2 for x in range(1, 6)]
evens = [x for x in range(1, 11) if x % 2 == 0]
```

#### 5. dict_demo.py
```python
user = {"name": "张三", "age": 25, "city": "上海"}
user["email"] = "zhang@example.com"

for key, value in user.items():
    print(f"{key}: {value}")

squares = {x: x**2 for x in range(1, 6)}
```

---

## 📅 周六（7/4）项目：通讯录管理

**完整代码在 `04_核心项目/W1_通讯录/contacts.py`**

---

## 🎯 使用方法

1. 每天打开对应日期的练习
2. 手动敲一遍（不要复制粘贴！）
3. 保存为 `.py` 文件
4. 运行测试
5. 周末一次性 push 到 GitHub

**遇到报错怎么办？**
- 截图报错信息
- 贴给 Claude，我帮你 debug

**记住：卡壳超过 30 分钟就跳过，不要硬磕！**